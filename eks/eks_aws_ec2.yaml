plugin: aws_ec2

regions:  
  - ap-northeast-1

cache: false

  #cache_max_age: 3600

filters:
  instance-state-name: running
  tags:
    eks:nodegroup-name:
      - project-nodegroup-web
      - project-nodegroup-was



keyed_groups:
  #  - prefix: tags
  #    key: tags
  - key: tags.eks:nodegroup-name
    separator: ''
hostnames:
  - network-interface.association.public-ip
  - network-interface.addresses.private-ip-address